{
    "name": "yewchat",
    "version": "1.0.0",
    "description": "",
    "main": "bootstrap.js",
    "scripts": {
        "build": "rimraf dist pkg && wasm-pack build --release --out-name yewchat -- --features wee_alloc && webpack",
        "start": "rimraf dist pkg && wasm-pack build --release --out-name yewchat -- --features wee_alloc && webpack serve --open --devtool eval",
        "test": "cargo test && wasm-pack test --headless"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "copy-webpack-plugin": "^10.2.0",
        "rimraf": "^3.0.2",
        "webpack": "^5.106.2",
        "webpack-cli": "^7.0.2",
        "webpack-dev-server": "^5.2.4"
    }
}
